services:
  # Backend Service
  - type: web
    name: startup-backend
    runtime: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: DB_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false

  # Frontend Service
  - type: static_site
    name: startup-frontend
    buildCommand: "" 
    staticPublishPath: ./frontend
    envVars:
      - key: API_URL
        value: https://startup-backend.onrender.com
